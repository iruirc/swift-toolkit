---
description: |
  Bootstrap a new multi-package SPM workspace.
  Создаёт новый multi-package SPM workspace.
---

Activate `swift-toolkit:workspace-init` and follow its flow. If `--from <path>` is supplied, run the batch mode. If `--resume` is supplied, run the resume mode. Otherwise, run the interactive Q&A.
