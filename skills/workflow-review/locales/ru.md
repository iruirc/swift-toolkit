# workflow-review — ru

## auto_move_prompt
Применить auto-move по `[REVIEW_STATUS]={status}`? [Yes / Modify / No]

## status_cancelled_user_no
Пользователь отклонил auto-move (нажал `No` в AUQ оркестратора)

## notes_template
[REVIEW_STATUS]={status}; auto-move: {action}
