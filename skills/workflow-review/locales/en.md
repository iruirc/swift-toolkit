# workflow-review — en

## auto_move_prompt
Apply auto-move per `[REVIEW_STATUS]={status}`? [Yes / Modify / No]

## status_cancelled_user_no
User declined the auto-move (pressed `No` in orchestrator AUQ)

## notes_template
[REVIEW_STATUS]={status}; auto-move: {action}
