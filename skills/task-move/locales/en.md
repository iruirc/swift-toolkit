# task-move — en

## error_task_not_found
Task `{task_id}` not found.

## error_invalid_status
Invalid status `{status}`. Allowed: TODO, ACTIVE, DONE, BACKLOG, RESEARCH, CHECK, UNABLE_FIX (root tasks); plus PENDING, IN_PROGRESS, DEFERRED, BLOCKED, SKIPPED (step tasks).

## report_moved_root
Moved task `{task_id}`: `{from}` → `{to}`.

## report_moved_step
Step `{step_id}` of epic `{parent_id}`: [STATUS] = `{from}` → `{to}`.
