## description
Перегенерирует marker-секции в meta-repo и per-package доках из workspace.yml + исходников.

## report_regenerated_files
Перегенерировано {n} файл(ов).

## report_no_drift
Дрейфа нет; все marker-секции в каноническом виде.

## report_drift_detected
Обнаружен дрейф в {n} файл(ах):

## error_malformed_markers
В {n} файл(ах) сломаны маркеры; запусти с --repair для интерактивного фикса. exit 2.

## error_missing_workspace_yml
workspace.yml не найден в cwd или предках; перейди в meta-repo workspace'а.

## repair_prompt
Применить? (y/N)
